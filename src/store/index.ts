/** Store barrel — zustand preference stores (docs/architecture.md §2, §3). */
export {
  usePreferences,
  DEFAULT_PREFERENCES,
  type Preferences,
  type ThemeMode,
  type WeekStart,
} from "./preferences";
export { remindersEnabled } from "./reminders";
